

# DB接続情報
# spring.datasource.url=${DB_URL:jdbc:oracle:thin:@//localhost:1521/ODB1}
spring.datasource.url=${DB_URL:jdbc:oracle:thin:@//172.24.169.13:1521/ODB1}
spring.datasource.username=${DB_USER:oracle}
spring.datasource.password=${DB_PASSWORD:password}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver

# spring.datasource.hikari.maximum-pool-size=100
# spring.datasource.hikari.transaction-isolation=TRANSACTION_READ_COMMITTED
# spring.datasource.hikari.transaction-isolation=TRANSACTION_REPEATABLE_READ  # errod
# spring.datasource.hikari.transaction-isolation=TRANSACTION_SERIALIZABLE


spring.sql.init.mode=always
spring.batch.jdbc.initialize-schema=always

# spring.batch.jdbc.schema=classpath:org/springframework/batch/core/schema-oracle10g.sql


