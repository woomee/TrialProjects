spring:
  devtools:
    restart:
      enabled: true 
      exclude: "static/**, public/**"
      additional-exclude: "**/*.xml" 
      additional-paths: ['src/main/webapp', 'src/main/resources']
      trigger-file: .reload
      
server:
  servlet:
    context-path: /TrialSpringBoot
  port: 1234
   

greeting:
   app: custom
